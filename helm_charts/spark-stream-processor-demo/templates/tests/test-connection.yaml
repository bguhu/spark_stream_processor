apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spark-stream-processor-demo.fullname" . }}-test-connection"
  labels:
    {{- include "spark-stream-processor-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spark-stream-processor-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
