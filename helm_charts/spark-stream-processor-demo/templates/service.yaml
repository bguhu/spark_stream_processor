apiVersion: v1
kind: Service
metadata:
  name: {{ include "spark-stream-processor-demo.fullname" . }}
  labels:
    {{- include "spark-stream-processor-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spark-stream-processor-demo.selectorLabels" . | nindent 4 }}
